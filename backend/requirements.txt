fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
groq>=0.12.0
joblib>=1.4.0
numpy>=1.26.0
pydantic>=2.9.0

# ML runtime + training
# (scikit-learn is required at runtime to unpickle the trained model;
# pandas is used by scripts/train_model.py and is light enough to ship.)
scikit-learn>=1.5.0
pandas>=2.2.0
